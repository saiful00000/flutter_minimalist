name: flutter_minimalist
description: A flutter utility package with some usefull extensions and commonly used widgets and functionality to minimize codebase and mantain readability.
issue_tracker: https://github.com/saiful00000/flutter_minimalist/issues
homepage: https://github.com/saiful00000/flutter_minimalist/blob/master/README.md

version: 0.0.2

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: